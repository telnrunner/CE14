//rafce
import { toast } from "sonner";

export const handleLogin = () => {
    toast("Login successfully!");
  };

export const handleLogout = () => {
    toast("Logout successfully!");
  };

//เดี่ยวจะใช้ต่อสำหรับการแจ้งเตือน login, register